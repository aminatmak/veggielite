
<div data-search-products-target="list">
  <p class="h3">
    <% if params[:query].blank? %>
      <%= pluralize products.count, 'product' %> available
    <% else %>
      <%= pluralize products.count, 'result' %> for <code><%= params[:query] %></code>
    <% end %>
  </p>
  <div class="products">
    <%# <% products.each do |product| %>
      <%= render 'product_infos', product: @products, cart: @cart %>
    <%# <% end %>
  </div>
</div>
